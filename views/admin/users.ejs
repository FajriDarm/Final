<!DOCTYPE html>
<html lang="id">

<head>
  <!-- Style untuk modal notifikasi -->
  <style>
    .modal-notification {
      display: none;
      position: fixed;
      inset: 0;
      z-index: 9999;
      align-items: center;
      justify-content: center;
      background: rgba(0, 0, 0, 0.2);
    }

    .modal-notification .modal-box {
      background: #fff;
      border-radius: 1rem;
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.15);
      max-width: 400px;
      width: 100%;
      padding: 2rem;
      text-align: center;
      animation: fadeIn 0.2s ease;
    }

    .modal-notification .notification-icon {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 64px;
      height: 64px;
      border-radius: 50%;
      box-shadow: 0 4px 24px 0 rgba(0, 0, 0, 0.15);
      margin-bottom: 1rem;
    }

    .modal-notification .notification-icon.success {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
    }

    .modal-notification .notification-icon.error {
      background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
    }

    .modal-notification .notification-icon.warning {
      background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
    }

    .modal-notification .notification-icon.info {
      background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
    }

    .modal-notification .notification-icon i {
      font-size: 1.5rem;
      color: white;
    }

    .modal-notification .notification-checkmark-svg {
      display: block;
    }

    .modal-notification .notification-checkmark-path {
      stroke-dasharray: 40;
      stroke-dashoffset: 40;
      animation: checkmark-draw 1.1s cubic-bezier(.28, .84, .42, 1) 0.15s forwards;
    }

    @keyframes checkmark-draw {
      to {
        stroke-dashoffset: 0;
      }
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: scale(0.95);
      }

      to {
        opacity: 1;
        transform: scale(1);
      }
    }
  </style>

  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manajemen Pengguna - Affillink Admin</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');

    body {
      font-family: 'Inter', sans-serif;
    }

    .glass-card {
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .gradient-primary {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
    }

    .gradient-secondary {
      background: linear-gradient(135deg, #34d399 0%, #10b981 100%);
    }

    .gradient-accent {
      background: linear-gradient(135deg, #6ee7b7 0%, #34d399 100%);
    }

    .gradient-dark {
      background: linear-gradient(135deg, #065f46 0%, #047857 100%);
    }

    .card-hover {
      transition: all 0.3s ease;
    }

    .card-hover:hover {
      transform: translateY(-5px);
      box-shadow: 0 20px 25px -5px rgba(5, 150, 105, 0.1), 0 10px 10px -5px rgba(5, 150, 105, 0.04);
    }

    .modal-overlay {
      backdrop-filter: blur(4px);
      background: rgba(0, 0, 0, 0.6);
    }

    .pulse-animation {
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0% {
        box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.4);
      }

      70% {
        box-shadow: 0 0 0 10px rgba(16, 185, 129, 0);
      }

      100% {
        box-shadow: 0 0 0 0 rgba(16, 185, 129, 0);
      }
    }

    .scrollbar-hide::-webkit-scrollbar {
      display: none;
    }

    .scrollbar-hide {
      -ms-overflow-style: none;
      scrollbar-width: none;
    }

    .success-badge {
      animation: slideIn 0.3s ease-out;
    }

    @keyframes slideIn {
      from {
        transform: translateY(-20px);
        opacity: 0;
      }

      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    .line-clamp-2 {
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }

    .user-avatar {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
    }

    .status-indicator {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      display: inline-block;
      margin-right: 6px;
    }

    .type-radio:checked+label {
      border-color: #10b981;
      background-color: #f0fdf4;
      box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.1);
    }
  </style>
</head>

<body class="bg-gradient-to-br from-emerald-50 to-green-50 min-h-screen">
  <%- include('../navbar/navbar.ejs', { name: 'Admin' }) %>
    <%- include('../sidebar/admin.ejs') %>

      <main class="ml-64 pt-16 min-h-screen pb-8">
        <div class="p-6">
          <!-- Header Halaman -->
          <div class="flex flex-col md:flex-row items-start md:items-center justify-between mb-8">
            <div class="mb-4 md:mb-0">
              <div class="flex items-center gap-3">
                <div class="w-12 h-12 gradient-primary rounded-xl flex items-center justify-center shadow-lg">
                  <i class="fas fa-users text-white text-xl"></i>
                </div>
                <div>
                  <h1 class="text-3xl font-bold text-gray-900">Manajemen Pengguna</h1>
                  <p class="text-gray-600 mt-1">Kelola semua pengguna sistem Affillink</p>
                </div>
              </div>
            </div>
            <div class="flex items-center gap-4">
              <div class="hidden md:block px-4 py-2 bg-white rounded-lg shadow-sm border border-green-100">
                <div class="flex items-center gap-2">
                  <i class="fas fa-calendar-alt text-green-600"></i>
                  <span class="text-sm font-medium text-gray-700">
                    <%= new Date().toLocaleDateString('id-ID', { weekday: 'long' , year: 'numeric' , month: 'long' ,
                      day: 'numeric' }) %>
                  </span>
                </div>
              </div>
              <div class="px-4 py-2 gradient-primary rounded-lg shadow-lg text-white pulse-animation">
                <div class="flex items-center gap-2">
                  <i class="fas fa-bell pulse-animation"></i>
                  <span class="text-sm font-medium">Update Real-time</span>
                </div>
              </div>
            </div>
          </div>

          <!-- Kartu Statistik -->
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
            <div class="glass-card rounded-2xl shadow-lg p-6 border border-green-100 card-hover">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-gray-600 text-sm font-medium mb-1">Total Pengguna</p>
                  <p class="text-3xl font-bold text-gray-900 mb-2" id="totalUsers">-</p>
                </div>
                <div class="w-14 h-14 gradient-primary rounded-xl flex items-center justify-center shadow-lg">
                  <i class="fas fa-users text-white text-2xl"></i>
                </div>
              </div>
              <div class="mt-4 pt-4 border-t border-green-50">
                <div class="flex items-center gap-2 text-sm">
                  <span class="text-green-600 font-medium flex items-center">
                    <i class="fas fa-chart-line mr-1"></i>
                    <span id="monthGrowth">+15%</span>
                  </span>
                  <span class="text-gray-500">dari bulan lalu</span>
                </div>
              </div>
            </div>

            <div class="glass-card rounded-2xl shadow-lg p-6 border border-green-100 card-hover">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-gray-600 text-sm font-medium mb-1">Pengguna Aktif</p>
                  <p class="text-3xl font-bold text-green-600 mb-2" id="activeUsers">-</p>
                </div>
                <div class="w-14 h-14 gradient-secondary rounded-xl flex items-center justify-center shadow-lg">
                  <i class="fas fa-user-check text-white text-2xl"></i>
                </div>
              </div>
              <div class="mt-4 pt-4 border-t border-green-50">
                <div class="flex items-center justify-between text-sm">
                  <span class="text-gray-600">Online sekarang</span>
                  <span class="font-medium text-green-700" id="onlineUsers">12</span>
                </div>
              </div>
            </div>

            <div class="glass-card rounded-2xl shadow-lg p-6 border border-green-100 card-hover">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-gray-600 text-sm font-medium mb-1">Affiliates</p>
                  <p class="text-3xl font-bold text-blue-600 mb-2" id="affiliateUsers">-</p>
                </div>
                <div
                  class="w-14 h-14 bg-gradient-to-br from-blue-500 to-blue-600 rounded-xl flex items-center justify-center shadow-lg">
                  <i class="fas fa-user-tie text-white text-2xl"></i>
                </div>
              </div>
              <div class="mt-4 pt-4 border-t border-green-50">
                <div class="flex items-center justify-between text-sm">
                  <span class="text-gray-600">Affiliates aktif</span>
                  <span class="font-medium text-blue-700" id="activeAffiliates">-</span>
                </div>
              </div>
            </div>

            <div class="glass-card rounded-2xl shadow-lg p-6 border border-green-100 card-hover">
              <div class="flex items-center justify-between">
                <div>
                  <p class="text-gray-600 text-sm font-medium mb-1">Affiliates Tertunda</p>
                  <p class="text-3xl font-bold text-amber-600 mb-2" id="pendingAffiliates">-</p>
                </div>
                <div
                  class="w-14 h-14 bg-gradient-to-br from-amber-500 to-amber-600 rounded-xl flex items-center justify-center shadow-lg">
                  <i class="fas fa-clock text-white text-2xl"></i>
                </div>
              </div>
              <div class="mt-4 pt-4 border-t border-green-50">
                <div class="flex items-center justify-between text-sm">
                  <span class="text-gray-600">Butuh persetujuan</span>
                  <span class="font-medium text-amber-700" id="pendingCount">-</span>
                </div>
              </div>
            </div>
          </div>

          <!-- Pencarian dan Filter -->
          <div class="glass-card rounded-2xl p-6 border border-green-100 mb-8">
            <div class="flex flex-col lg:flex-row lg:items-center justify-between gap-6">
              <div class="flex-1">
                <div class="relative">
                  <i class="fas fa-search absolute left-4 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
                  <input type="text" id="searchInput" placeholder="Cari pengguna berdasarkan nama, email..."
                    class="w-full pl-12 pr-4 py-3 border border-green-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent bg-white shadow-sm transition-all">
                </div>
              </div>

              <div class="flex flex-wrap gap-4">
                <div class="relative">
                  <i class="fas fa-user-tag absolute left-4 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
                  <select id="roleFilter"
                    class="pl-12 pr-4 py-3 border border-green-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent bg-white shadow-sm appearance-none min-w-[180px]">
                    <option value="">Semua Peran</option>
                    <option value="super_admin">Super Admin</option>
                    <option value="sales">Sales</option>
                    <option value="finance">Finance</option>
                    <option value="affiliate">Affiliate</option>
                    <option value="user">Pengguna</option>
                  </select>
                </div>

                <div class="relative">
                  <i class="fas fa-filter absolute left-4 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
                  <select id="statusFilter"
                    class="pl-12 pr-4 py-3 border border-green-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent bg-white shadow-sm appearance-none min-w-[180px]">
                    <option value="">Semua Status</option>
                    <option value="active">Aktif</option>
                    <option value="inactive">Tidak Aktif</option>
                  </select>
                </div>

                <button onclick="clearFilters()"
                  class="px-4 py-3 border border-green-200 text-green-700 hover:bg-green-50 rounded-xl transition-colors flex items-center gap-2">
                  <i class="fas fa-redo"></i>
                  <span class="font-medium">Hapus Filter</span>
                </button>
              </div>
            </div>
          </div>

          <!-- Grid Pengguna -->
          <div id="usersContainer" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
            <div class="col-span-full text-center py-12 text-gray-400">
              <div class="flex flex-col items-center">
                <div
                  class="w-16 h-16 gradient-primary rounded-full flex items-center justify-center mb-4 animate-pulse">
                  <i class="fas fa-spinner fa-spin text-2xl text-white"></i>
                </div>
                <p class="text-lg font-medium">Memuat pengguna...</p>
              </div>
            </div>
          </div>
        </div>
      </main>

      <!-- Modal Lihat Pengguna -->
      <div id="viewModal" class="fixed inset-0 modal-overlay hidden items-center justify-center z-50 p-4">
        <div class="bg-white rounded-2xl shadow-2xl max-w-md w-full max-h-[90vh] overflow-hidden flex flex-col">
          <!-- Header Modal -->
          <div class="gradient-primary px-6 py-4 flex items-center justify-between">
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 bg-white/20 rounded-lg flex items-center justify-center">
                <i class="fas fa-user text-white"></i>
              </div>
              <div>
                <h3 class="text-xl font-bold text-white">Detail Pengguna</h3>
                <p class="text-green-100 text-sm mt-1">Informasi lengkap pengguna</p>
              </div>
            </div>
            <button onclick="closeViewModal()"
              class="w-10 h-10 bg-white/20 hover:bg-white/30 rounded-lg flex items-center justify-center transition-colors">
              <i class="fas fa-times text-white"></i>
            </button>
          </div>

          <!-- Body Modal -->
          <div class="p-6 overflow-y-auto flex-1 scrollbar-hide">
            <!-- Header Info Pengguna -->
            <div class="flex flex-col items-center text-center mb-6">
              <div id="viewUserAvatar"
                class="w-24 h-24 user-avatar rounded-full flex items-center justify-center text-white text-3xl font-bold mb-4 shadow-lg">
                <!-- Avatar akan diisi secara dinamis -->
              </div>
              <h2 id="viewUserName" class="text-2xl font-bold text-gray-900 mb-1"></h2>
              <p id="viewUserEmail" class="text-gray-600 mb-3"></p>
              <p class="text-sm text-gray-500">ID Pengguna: <span id="viewUserID" class="font-medium">-</span></p>
            </div>

            <!-- Detail Pengguna -->
            <div class="space-y-4 bg-gray-50 rounded-xl p-5">
              <!-- Peran -->
              <div class="flex items-center justify-between">
                <div class="flex items-center gap-3">
                  <div class="w-10 h-10 bg-white rounded-lg flex items-center justify-center shadow-sm">
                    <i class="fas fa-user-tag text-green-600"></i>
                  </div>
                  <div>
                    <p class="text-sm text-gray-500">Peran</p>
                    <p id="viewUserRole" class="font-semibold text-gray-900"></p>
                  </div>
                </div>
                <span id="viewRoleBadge" class="px-3 py-1.5 rounded-full text-xs font-semibold"></span>
              </div>

              <!-- Status -->
              <div class="flex items-center justify-between">
                <div class="flex items-center gap-3">
                  <div class="w-10 h-10 bg-white rounded-lg flex items-center justify-center shadow-sm">
                    <i class="fas fa-toggle-on text-green-600"></i>
                  </div>
                  <div>
                    <p class="text-sm text-gray-500">Status Akun</p>
                    <p id="viewUserStatus" class="font-semibold text-gray-900"></p>
                  </div>
                </div>
                <span id="viewStatusBadge"
                  class="px-3 py-1.5 rounded-full text-xs font-semibold flex items-center gap-2">
                  <!-- Konten badge akan diisi secara dinamis -->
                </span>
              </div>

              <!-- Status Affiliate -->
              <div class="flex items-center justify-between">
                <div class="flex items-center gap-3">
                  <div class="w-10 h-10 bg-white rounded-lg flex items-center justify-center shadow-sm">
                    <i class="fas fa-user-tie text-green-600"></i>
                  </div>
                  <div>
                    <p class="text-sm text-gray-500">Status Affiliate</p>
                    <p id="viewAffiliateStatus" class="font-semibold text-gray-900"></p>
                  </div>
                </div>
                <span id="viewAffiliateBadge"
                  class="px-3 py-1.5 rounded-full text-xs font-semibold flex items-center gap-2">
                  <!-- Konten badge akan diisi secara dinamis -->
                </span>
              </div>

              <!-- Tanggal Bergabung -->
              <div class="flex items-center justify-between">
                <div class="flex items-center gap-3">
                  <div class="w-10 h-10 bg-white rounded-lg flex items-center justify-center shadow-sm">
                    <i class="fas fa-calendar-plus text-green-600"></i>
                  </div>
                  <div>
                    <p class="text-sm text-gray-500">Tanggal Bergabung</p>
                    <p id="viewJoinDate" class="font-semibold text-gray-900"></p>
                  </div>
                </div>
                <i class="fas fa-history text-gray-400"></i>
              </div>

              <!-- Login Terakhir -->
              <div class="flex items-center justify-between">
                <div class="flex items-center gap-3">
                  <div class="w-10 h-10 bg-white rounded-lg flex items-center justify-center shadow-sm">
                    <i class="fas fa-sign-in-alt text-green-600"></i>
                  </div>
                  <div>
                    <p class="text-sm text-gray-500">Login Terakhir</p>
                    <p id="viewLastLogin" class="font-semibold text-gray-900">-</p>
                  </div>
                </div>
                <i class="fas fa-clock text-gray-400"></i>
              </div>
            </div>

            <!-- Informasi Tambahan -->
            <div class="mt-6 pt-6 border-t border-gray-200">
              <h4 class="text-sm font-semibold text-gray-700 mb-4 flex items-center gap-2">
                <i class="fas fa-info-circle text-green-600"></i>
                Informasi Tambahan
              </h4>
              <div class="grid grid-cols-2 gap-4">
                <div class="bg-green-50 rounded-lg p-4 text-center">
                  <p class="text-2xl font-bold text-green-700" id="viewTotalReferrals">0</p>
                  <p class="text-sm text-green-600 mt-1">Referral</p>
                </div>
                <div class="bg-blue-50 rounded-lg p-4 text-center">
                  <p class="text-2xl font-bold text-blue-700" id="viewTotalCommissions">0</p>
                  <p class="text-sm text-blue-600 mt-1">Komisi</p>
                </div>
              </div>
            </div>
          </div>

          <!-- Footer Modal -->
          <div class="border-t border-gray-200 px-6 py-4 bg-gray-50 flex gap-4">
            <button onclick="closeViewModal()"
              class="flex-1 bg-gray-200 text-gray-700 py-3.5 rounded-xl hover:bg-gray-300 transition-all font-semibold flex items-center justify-center gap-2">
              <i class="fas fa-times"></i>
              Tutup
            </button>
            <button onclick="editFromView()" id="editFromViewBtn"
              class="flex-1 gradient-primary text-white py-3.5 rounded-xl hover:opacity-90 transition-all font-semibold shadow-lg flex items-center justify-center gap-2">
              <i class="fas fa-edit"></i>
              Edit Pengguna
            </button>
          </div>
        </div>
      </div>

      <!-- Modal Edit Pengguna -->
      <div id="editModal" class="fixed inset-0 modal-overlay hidden items-center justify-center z-50 p-4">
        <div class="bg-white rounded-2xl shadow-2xl max-w-md w-full max-h-[90vh] overflow-hidden flex flex-col">
          <!-- Header Modal -->
          <div class="gradient-primary px-6 py-4 flex items-center justify-between">
            <div class="flex items-center gap-3">
              <div class="w-10 h-10 bg-white/20 rounded-lg flex items-center justify-center">
                <i class="fas fa-user-edit text-white"></i>
              </div>
              <div>
                <h3 class="text-xl font-bold text-white">Edit Pengguna</h3>
                <p class="text-green-100 text-sm mt-1">Perbarui informasi pengguna</p>
              </div>
            </div>
            <button onclick="closeEditModal()"
              class="w-10 h-10 bg-white/20 hover:bg-white/30 rounded-lg flex items-center justify-center transition-colors">
              <i class="fas fa-times text-white"></i>
            </button>
          </div>

          <!-- Body Modal -->
          <div class="p-6 overflow-y-auto flex-1 scrollbar-hide">
            <form id="editForm" class="space-y-6">
              <input type="hidden" id="editUserId">

              <!-- Info Pengguna -->
              <div class="flex flex-col items-center text-center mb-6">
                <div id="editUserAvatar"
                  class="w-20 h-20 user-avatar rounded-full flex items-center justify-center text-white text-2xl font-bold mb-4">
                  <!-- Avatar akan diisi secara dinamis -->
                </div>
                <p class="text-sm text-gray-500">ID Pengguna: <span id="editUserID" class="font-medium">-</span></p>
              </div>

              <!-- Field Form -->
              <div class="space-y-4">
                <!-- Nama -->
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">
                    <i class="fas fa-user mr-2 text-green-600"></i>
                    Nama
                  </label>
                  <input type="text" id="editName" required
                    class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all bg-white">
                </div>

                <!-- Email -->
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">
                    <i class="fas fa-envelope mr-2 text-green-600"></i>
                    Email
                  </label>
                  <input type="email" id="editEmail" required
                    class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all bg-white">
                </div>

                <!-- Peran -->
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">
                    <i class="fas fa-user-tag mr-2 text-green-600"></i>
                    Peran
                  </label>
                  <select id="editRole"
                    class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all bg-white">
                    <option value="user">Pengguna</option>
                    <option value="super_admin">Super Admin</option>
                    <option value="sales">Sales</option>
                    <option value="finance">Finance</option>
                    <option value="affiliate">Affiliate</option>
                  </select>
                </div>

                <!-- Status -->
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">
                    <i class="fas fa-toggle-on mr-2 text-green-600"></i>
                    Status
                  </label>
                  <div class="grid grid-cols-2 gap-4">
                    <label class="cursor-pointer">
                      <input type="radio" name="editStatus" value="active" class="peer sr-only">
                      <div
                        class="p-4 border-2 border-gray-200 rounded-xl hover:border-green-300 peer-checked:border-green-500 peer-checked:bg-green-50 transition-all text-center">
                        <div class="w-8 h-8 bg-green-100 rounded-lg flex items-center justify-center mx-auto mb-2">
                          <i class="fas fa-check-circle text-green-600"></i>
                        </div>
                        <p class="font-medium text-gray-900">Aktif</p>
                      </div>
                    </label>
                    <label class="cursor-pointer">
                      <input type="radio" name="editStatus" value="inactive" class="peer sr-only">
                      <div
                        class="p-4 border-2 border-gray-200 rounded-xl hover:border-red-300 peer-checked:border-red-500 peer-checked:bg-red-50 transition-all text-center">
                        <div class="w-8 h-8 bg-red-100 rounded-lg flex items-center justify-center mx-auto mb-2">
                          <i class="fas fa-times-circle text-red-600"></i>
                        </div>
                        <p class="font-medium text-gray-900">Tidak Aktif</p>
                      </div>
                    </label>
                  </div>
                </div>

                <!-- Status Affiliate -->
                <div id="affiliateStatusSection" class="hidden">
                  <label class="block text-sm font-semibold text-gray-700 mb-2">
                    <i class="fas fa-user-tie mr-2 text-green-600"></i>
                    Status Affiliate
                  </label>
                  <select id="editAffiliateStatus"
                    class="w-full px-4 py-3 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all bg-white">
                    <option value="inactive">Tidak Aktif</option>
                    <option value="pending">Tertunda</option>
                    <option value="approved">Disetujui</option>
                    <option value="rejected">Ditolak</option>
                  </select>
                </div>
              </div>
            </form>
          </div>

          <!-- Footer Modal -->
          <div class="border-t border-gray-200 px-6 py-4 bg-gray-50 flex gap-4">
            <button type="button" onclick="closeEditModal()"
              class="flex-1 bg-gray-200 text-gray-700 py-3.5 rounded-xl hover:bg-gray-300 transition-all font-semibold flex items-center justify-center gap-2">
              <i class="fas fa-times"></i>
              Batal
            </button>
            <button type="submit" form="editForm"
              class="flex-1 gradient-primary text-white py-3.5 rounded-xl hover:opacity-90 transition-all font-semibold shadow-lg flex items-center justify-center gap-2">
              <i class="fas fa-save"></i>
              Simpan Perubahan
            </button>
          </div>
        </div>
      </div>

      <!-- Modal Notifikasi -->
      <div id="notificationModal" class="modal-notification">
        <div class="modal-box">
          <span class="notification-icon success" id="notificationIcon">
            <svg class="notification-checkmark-svg" width="48" height="48" viewBox="0 0 48 48">
              <circle cx="24" cy="24" r="22" fill="none" stroke="#fff" stroke-width="4" opacity="0.15" />
              <path class="notification-checkmark-path" d="M14 25.5L22 33L34 17" fill="none" stroke="#fff"
                stroke-width="4" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
          </span>
          <h2 class="text-xl font-bold text-gray-900 mb-2" id="notificationTitle">Sukses</h2>
          <p class="text-gray-600 mb-6" id="notificationMessage">Operasi berhasil dilakukan</p>
          <button id="closeNotificationModalBtn" type="button"
            class="w-full bg-gradient-to-r from-green-600 to-emerald-600 text-white py-3.5 px-6 rounded-xl font-medium hover:from-green-700 hover:to-emerald-700 transition-all duration-200">
            Tutup
          </button>
        </div>
      </div>

      <!-- Modal Konfirmasi -->
      <div id="confirmationModal" class="modal-notification" style="display: none;">
        <div class="modal-box">
          <span class="notification-icon warning" id="confirmationIcon">
            <i class="fas fa-exclamation-triangle"></i>
          </span>
          <h2 class="text-xl font-bold text-gray-900 mb-2" id="confirmationTitle">Konfirmasi</h2>
          <p class="text-gray-600 mb-6" id="confirmationMessage">Apakah Anda yakin?</p>
          <div class="flex gap-3">
            <button id="cancelConfirmationBtn" type="button"
              class="flex-1 bg-gray-200 text-gray-700 py-3 px-6 rounded-xl font-medium hover:bg-gray-300 transition-all duration-200">
              Batal
            </button>
            <button id="confirmActionBtn" type="button"
              class="flex-1 bg-gradient-to-r from-yellow-500 to-amber-500 text-white py-3 px-6 rounded-xl font-medium hover:opacity-90 transition-all duration-200">
              Ya
            </button>
          </div>
        </div>
      </div>

      <!-- Modal Hapus Pengguna -->
      <div id="deleteModal" class="modal-notification" style="display: none;">
        <div class="modal-box">
          <span class="notification-icon error" id="deleteIcon">
            <i class="fas fa-trash-alt"></i>
          </span>
          <h2 class="text-xl font-bold text-gray-900 mb-2">Hapus Pengguna</h2>
          <p class="text-gray-600 mb-6" id="deleteMessage">Apakah Anda yakin ingin menghapus pengguna ini? Tindakan ini
            tidak dapat dibatalkan.</p>
          <div class="flex gap-3">
            <button id="cancelDeleteBtn" type="button"
              class="flex-1 bg-gray-200 text-gray-700 py-3 px-6 rounded-xl font-medium hover:bg-gray-300 transition-all duration-200">
              Batal
            </button>
            <button id="confirmDeleteBtn" type="button"
              class="flex-1 bg-gradient-to-r from-red-600 to-rose-600 text-white py-3 px-6 rounded-xl font-medium hover:opacity-90 transition-all duration-200">
              Hapus
            </button>
          </div>
        </div>
      </div>

      <script>
        let allUsers = [];
        let currentUserId = null;
        let confirmationCallback = null;
        let deleteCallback = null;

        // ============================================================
        // FUNGSI NOTIFIKASI MODAL
        // ============================================================

        /**
         * Tampilkan modal notifikasi dengan berbagai tipe
         */
        function showNotification(type, title, message, autoClose = true) {
          const modal = document.getElementById('notificationModal');
          const icon = document.getElementById('notificationIcon');
          const titleEl = document.getElementById('notificationTitle');
          const messageEl = document.getElementById('notificationMessage');
          const closeBtn = document.getElementById('closeNotificationModalBtn');

          // Reset kelas icon
          icon.className = 'notification-icon';

          // Konfigurasi untuk setiap tipe notifikasi
          const configs = {
            success: {
              iconClass: 'success',
              iconHTML: `
                <svg class="notification-checkmark-svg" width="48" height="48" viewBox="0 0 48 48">
                  <circle cx="24" cy="24" r="22" fill="none" stroke="#fff" stroke-width="4" opacity="0.15" />
                  <path class="notification-checkmark-path" d="M14 25.5L22 33L34 17" fill="none" stroke="#fff" stroke-width="4"
                    stroke-linecap="round" stroke-linejoin="round" />
                </svg>
              `,
              buttonClass: 'from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700'
            },
            error: {
              iconClass: 'error',
              iconHTML: '<i class="fas fa-times-circle"></i>',
              buttonClass: 'from-red-600 to-rose-600 hover:from-red-700 hover:to-rose-700'
            },
            warning: {
              iconClass: 'warning',
              iconHTML: '<i class="fas fa-exclamation-triangle"></i>',
              buttonClass: 'from-yellow-600 to-amber-600 hover:from-yellow-700 hover:to-amber-700'
            },
            info: {
              iconClass: 'info',
              iconHTML: '<i class="fas fa-info-circle"></i>',
              buttonClass: 'from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700'
            },
            edit: {
              iconClass: 'success',
              iconHTML: '<i class="fas fa-user-edit"></i>',
              buttonClass: 'from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700'
            },
            delete: {
              iconClass: 'error',
              iconHTML: '<i class="fas fa-user-times"></i>',
              buttonClass: 'from-red-600 to-rose-600 hover:from-red-700 hover:to-rose-700'
            }
          };

          const config = configs[type] || configs.info;

          icon.classList.add(config.iconClass);
          icon.innerHTML = config.iconHTML;
          titleEl.textContent = title;
          messageEl.textContent = message;

          // Update warna tombol berdasarkan tipe
          closeBtn.className = `w-full bg-gradient-to-r ${config.buttonClass} text-white py-3.5 px-6 rounded-xl font-medium transition-all duration-200`;

          // Reset animasi checkmark untuk notifikasi sukses
          if (type === 'success' || type === 'edit') {
            const checkmarkPath = icon.querySelector('.notification-checkmark-path');
            if (checkmarkPath) {
              checkmarkPath.style.strokeDasharray = '40';
              checkmarkPath.style.strokeDashoffset = '40';
              setTimeout(() => {
                checkmarkPath.style.animation = 'none';
                setTimeout(() => {
                  checkmarkPath.style.animation = 'checkmark-draw 1.1s cubic-bezier(.28, .84, .42, 1) 0.15s forwards';
                }, 10);
              }, 10);
            }
          }

          modal.style.display = 'flex';

          if (autoClose) {
            setTimeout(() => {
              closeNotification();
            }, 4000);
          }
        }

        /**
         * Tampilkan modal konfirmasi dengan callback
         */
        function showConfirmationModal(type, title, message, confirmText, cancelText, onConfirm, onCancel = null) {
          const modal = document.getElementById('confirmationModal');
          const icon = document.getElementById('confirmationIcon');
          const titleEl = document.getElementById('confirmationTitle');
          const messageEl = document.getElementById('confirmationMessage');
          const confirmBtn = document.getElementById('confirmActionBtn');
          const cancelBtn = document.getElementById('cancelConfirmationBtn');

          // Konfigurasi icon berdasarkan tipe
          const iconConfigs = {
            approve: {
              iconClass: 'warning',
              iconHTML: '<i class="fas fa-user-check"></i>',
              buttonClass: 'from-green-600 to-emerald-600'
            },
            reject: {
              iconClass: 'warning',
              iconHTML: '<i class="fas fa-user-times"></i>',
              buttonClass: 'from-red-600 to-rose-600'
            }
          };

          const config = iconConfigs[type] || { iconClass: 'warning', iconHTML: '<i class="fas fa-exclamation-triangle"></i>', buttonClass: 'from-yellow-500 to-amber-500' };

          icon.className = 'notification-icon';
          icon.classList.add(config.iconClass);
          icon.innerHTML = config.iconHTML;

          titleEl.textContent = title;
          messageEl.textContent = message;
          confirmBtn.textContent = confirmText;
          cancelBtn.textContent = cancelText;

          // Update warna tombol konfirmasi
          confirmBtn.className = `flex-1 bg-gradient-to-r ${config.buttonClass} text-white py-3 px-6 rounded-xl font-medium hover:opacity-90 transition-all duration-200`;

          // Simpan callback
          confirmationCallback = onConfirm;

          // Setup event listeners
          confirmBtn.onclick = function (e) {
            e.preventDefault();
            closeConfirmationModal();
            if (onConfirm) onConfirm();
          };

          cancelBtn.onclick = function (e) {
            e.preventDefault();
            closeConfirmationModal();
            if (onCancel) onCancel();
          };

          modal.style.display = 'flex';

          // Tutup dengan klik di luar
          modal.addEventListener('click', function (e) {
            if (e.target === modal) {
              closeConfirmationModal();
              if (onCancel) onCancel();
            }
          });

          // Tutup dengan ESC key
          const escHandler = function (e) {
            if (e.key === 'Escape') {
              closeConfirmationModal();
              if (onCancel) onCancel();
              document.removeEventListener('keydown', escHandler);
            }
          };
          document.addEventListener('keydown', escHandler);
        }

        /**
         * Tampilkan modal konfirmasi hapus
         */
        function showDeleteModal(userName, onConfirm, onCancel = null) {
          const modal = document.getElementById('deleteModal');
          const messageEl = document.getElementById('deleteMessage');
          const confirmBtn = document.getElementById('confirmDeleteBtn');
          const cancelBtn = document.getElementById('cancelDeleteBtn');

          messageEl.textContent = `Apakah Anda yakin ingin menghapus pengguna "${userName}"? Tindakan ini tidak dapat dibatalkan.`;

          // Simpan callback
          deleteCallback = onConfirm;

          // Setup event listeners
          confirmBtn.onclick = function (e) {
            e.preventDefault();
            closeDeleteModal();
            if (onConfirm) onConfirm();
          };

          cancelBtn.onclick = function (e) {
            e.preventDefault();
            closeDeleteModal();
            if (onCancel) onCancel();
          };

          modal.style.display = 'flex';

          // Tutup dengan klik di luar
          modal.addEventListener('click', function (e) {
            if (e.target === modal) {
              closeDeleteModal();
              if (onCancel) onCancel();
            }
          });

          // Tutup dengan ESC key
          const escHandler = function (e) {
            if (e.key === 'Escape') {
              closeDeleteModal();
              if (onCancel) onCancel();
              document.removeEventListener('keydown', escHandler);
            }
          };
          document.addEventListener('keydown', escHandler);
        }

        /**
         * Tutup modal konfirmasi
         */
        function closeConfirmationModal() {
          const modal = document.getElementById('confirmationModal');
          modal.style.display = 'none';
        }

        /**
         * Tutup modal hapus
         */
        function closeDeleteModal() {
          const modal = document.getElementById('deleteModal');
          modal.style.display = 'none';
        }

        /**
         * Tutup modal notifikasi
         */
        function closeNotification() {
          const modal = document.getElementById('notificationModal');
          modal.style.display = 'none';
        }

        /**
         * Tampilkan notifikasi sukses
         */
        function showSuccess(message, title = 'Sukses') {
          showNotification('success', title, message);
        }

        /**
         * Tampilkan notifikasi error
         */
        function showError(message, title = 'Error') {
          showNotification('error', title, message);
        }

        // ============================================================
        // FUNGSI UTAMA MANAJEMEN PENGGUNA
        // ============================================================

        async function loadUsers() {
          try {
            const response = await fetch('/api/admin/users');
            const data = await response.json();

            if (data.success) {
              allUsers = data.data;
              updateStats(data.data);
              displayUsers(data.data);
            }
          } catch (error) {
            console.error('Error loading users:', error);
            showError('Gagal memuat pengguna');
          }
        }

        function updateStats(users) {
          const total = users.length;
          const active = users.filter(u => u.status === 'active').length;
          const affiliates = users.filter(u => u.affiliate_status === 'approved').length;
          const pending = users.filter(u => u.affiliate_status === 'pending').length;
          const activeAffiliates = users.filter(u => u.affiliate_status === 'approved' && u.status === 'active').length;

          document.getElementById('totalUsers').textContent = total;
          document.getElementById('activeUsers').textContent = active;
          document.getElementById('affiliateUsers').textContent = affiliates;
          document.getElementById('pendingAffiliates').textContent = pending;
          document.getElementById('activeAffiliates').textContent = activeAffiliates;
          document.getElementById('pendingCount').textContent = pending;
        }

        function displayUsers(users) {
          const container = document.getElementById('usersContainer');

          if (!users || users.length === 0) {
            container.innerHTML = `
          <div class="col-span-full text-center py-16">
            <div class="flex flex-col items-center">
              <div class="w-24 h-24 bg-gray-100 rounded-full flex items-center justify-center mb-6">
                <i class="fas fa-users text-4xl text-gray-300"></i>
              </div>
              <h3 class="text-xl font-semibold text-gray-700 mb-2">Tidak Ada Pengguna Ditemukan</h3>
              <p class="text-gray-500 max-w-md mx-auto">Tidak ada pengguna yang cocok dengan kriteria pencarian Anda.</p>
              <button onclick="clearFilters()" class="mt-6 px-6 py-3 gradient-primary text-white rounded-xl hover:opacity-90 transition-all font-semibold flex items-center gap-2">
                <i class="fas fa-redo"></i>
                Hapus Filter
              </button>
            </div>
          </div>
        `;
            return;
          }

          container.innerHTML = users.map(user => {
            const roleConfig = {
              'super_admin': { bg: 'bg-gradient-to-br from-purple-500 to-purple-600', text: 'text-purple-100', icon: 'fas fa-crown', badge: 'Super Admin' },
              'sales': { bg: 'bg-gradient-to-br from-blue-500 to-blue-600', text: 'text-blue-100', icon: 'fas fa-chart-line', badge: 'Sales' },
              'finance': { bg: 'bg-gradient-to-br from-green-500 to-green-600', text: 'text-green-100', icon: 'fas fa-coins', badge: 'Finance' },
              'affiliate': { bg: 'bg-gradient-to-br from-orange-500 to-orange-600', text: 'text-orange-100', icon: 'fas fa-user-tie', badge: 'Affiliate' },
              'user': { bg: 'bg-gradient-to-br from-gray-500 to-gray-600', text: 'text-gray-100', icon: 'fas fa-user', badge: 'Pengguna' }
            }[user.role_name] || { bg: 'bg-gradient-to-br from-gray-500 to-gray-600', text: 'text-gray-100', icon: 'fas fa-user', badge: 'Pengguna' };

            const affiliateStatusConfig = {
              'inactive': { bg: 'bg-gray-100', text: 'text-gray-800', icon: 'fas fa-minus-circle' },
              'pending': { bg: 'bg-yellow-100', text: 'text-yellow-800', icon: 'fas fa-clock' },
              'approved': { bg: 'bg-green-100', text: 'text-green-800', icon: 'fas fa-check-circle' },
              'rejected': { bg: 'bg-red-100', text: 'text-red-800', icon: 'fas fa-times-circle' }
            }[user.affiliate_status] || { bg: 'bg-gray-100', text: 'text-gray-800', icon: 'fas fa-minus-circle' };

            const statusConfig = {
              'active': { bg: 'bg-green-100', text: 'text-green-800', icon: 'fas fa-check-circle' },
              'inactive': { bg: 'bg-red-100', text: 'text-red-800', icon: 'fas fa-times-circle' }
            }[user.status] || { bg: 'bg-gray-100', text: 'text-gray-800', icon: 'fas fa-question-circle' };

            const joinDate = user.created_at ? new Date(user.created_at).toLocaleDateString('id-ID', { day: 'numeric', month: 'short', year: 'numeric' }) : '-';

            return `
          <div class="card-hover bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden group">
            <!-- Header dengan avatar -->
            <div class="relative h-32 ${roleConfig.bg} flex items-center justify-center">
              <div class="w-20 h-20 bg-white/20 backdrop-blur-sm rounded-full flex items-center justify-center border-4 border-white/30">
                <span class="text-2xl font-bold text-white">${user.name.charAt(0).toUpperCase()}</span>
              </div>
              <div class="absolute top-4 right-4 flex gap-2 opacity-0 group-hover:opacity-100 transition-opacity">
                <button onclick="editUser(${user.id})" class="w-9 h-9 rounded-lg bg-white/20 backdrop-blur-sm text-white hover:bg-white/30 transition-colors flex items-center justify-center">
                  <i class="fas fa-edit"></i>
                </button>
                <button onclick="deleteUser(${user.id})" class="w-9 h-9 rounded-lg bg-white/20 backdrop-blur-sm text-white hover:bg-white/30 transition-colors flex items-center justify-center">
                  <i class="fas fa-trash"></i>
                </button>
              </div>
            </div>

            <!-- Konten -->
            <div class="p-6">
              <div class="flex items-center justify-between mb-3">
                <div>
                  <h3 class="font-bold text-lg text-gray-900">${user.name}</h3>
                  <div class="flex items-center gap-1 text-sm text-gray-500 mt-1">
                    <i class="fas fa-envelope text-gray-400"></i>
                    <span class="truncate">${user.email}</span>
                  </div>
                </div>
                <span class="px-3 py-1.5 rounded-full text-xs font-semibold ${roleConfig.text} ${roleConfig.bg} flex items-center gap-2">
                  <i class="${roleConfig.icon} text-xs"></i>
                  ${roleConfig.badge}
                </span>
              </div>

              <div class="space-y-3">
                <div class="flex items-center justify-between text-sm">
                  <span class="text-gray-500 flex items-center gap-1">
                    <i class="fas fa-id-card text-gray-400"></i>
                    ID Pengguna
                  </span>
                  <span class="font-semibold text-gray-900">#${user.id}</span>
                </div>

                <div class="flex items-center justify-between text-sm">
                  <span class="text-gray-500 flex items-center gap-1">
                    <i class="fas fa-user-tie text-gray-400"></i>
                    Status Affiliate
                  </span>
                  <span class="font-semibold ${affiliateStatusConfig.text} bg-white px-3 py-1.5 rounded-full text-xs border ${affiliateStatusConfig.text.replace('text-', 'border-')}/20 flex items-center gap-2">
                    <i class="${affiliateStatusConfig.icon}"></i>
                    ${user.affiliate_status ? user.affiliate_status.charAt(0).toUpperCase() + user.affiliate_status.slice(1) : 'Tidak Aktif'}
                  </span>
                </div>

                <div class="flex items-center justify-between text-sm">
                  <span class="text-gray-500 flex items-center gap-1">
                    <i class="fas fa-toggle-on text-gray-400"></i>
                    Status
                  </span>
                  <span class="font-semibold ${statusConfig.text} bg-white px-3 py-1.5 rounded-full text-xs border ${statusConfig.text.replace('text-', 'border-')}/20 flex items-center gap-2">
                    <i class="${statusConfig.icon}"></i>
                    ${user.status.charAt(0).toUpperCase() + user.status.slice(1)}
                  </span>
                </div>

                <div class="flex items-center justify-between text-sm pt-2 border-t border-gray-100">
                  <span class="text-gray-500 flex items-center gap-1">
                    <i class="fas fa-calendar-plus text-gray-400"></i>
                    Bergabung
                  </span>
                  <span class="text-gray-900 font-medium">${joinDate}</span>
                </div>
              </div>
            </div>

            <!-- Footer dengan aksi cepat -->
            <div class="px-6 py-4 bg-gray-50 border-t border-gray-100 flex gap-3">
              <button onclick="viewUser(${user.id})" class="flex-1 text-sm font-semibold text-gray-600 hover:text-gray-800 py-2.5 rounded-lg hover:bg-gray-100 transition-colors flex items-center justify-center gap-2">
                <i class="fas fa-eye"></i>
                Lihat
              </button>
              <div class="w-px bg-gray-300"></div>
              <button onclick="editUser(${user.id})" class="flex-1 text-sm font-semibold text-blue-600 hover:text-blue-800 py-2.5 rounded-lg hover:bg-blue-50 transition-colors flex items-center justify-center gap-2">
                <i class="fas fa-edit"></i>
                Edit
              </button>
            </div>
          </div>
        `;
          }).join('');
        }

        function viewUser(userId) {
          const user = allUsers.find(u => u.id === userId);
          if (user) {
            // Set informasi dasar
            document.getElementById('viewUserID').textContent = user.id;
            document.getElementById('viewUserName').textContent = user.name;
            document.getElementById('viewUserEmail').textContent = user.email;

            // Set avatar
            const avatar = document.getElementById('viewUserAvatar');
            avatar.textContent = user.name.charAt(0).toUpperCase();
            avatar.className = 'w-24 h-24 user-avatar rounded-full flex items-center justify-center text-white text-3xl font-bold mb-4 shadow-lg';

            // Set peran
            document.getElementById('viewUserRole').textContent = user.role_name || 'Pengguna';

            const roleConfig = {
              'super_admin': { bg: 'bg-gradient-to-br from-purple-500 to-purple-600', text: 'text-white', badge: 'Super Admin' },
              'sales': { bg: 'bg-gradient-to-br from-blue-500 to-blue-600', text: 'text-white', badge: 'Sales' },
              'finance': { bg: 'bg-gradient-to-br from-green-500 to-green-600', text: 'text-white', badge: 'Finance' },
              'affiliate': { bg: 'bg-gradient-to-br from-orange-500 to-orange-600', text: 'text-white', badge: 'Affiliate' },
              'user': { bg: 'bg-gradient-to-br from-gray-500 to-gray-600', text: 'text-white', badge: 'Pengguna' }
            }[user.role_name] || { bg: 'bg-gradient-to-br from-gray-500 to-gray-600', text: 'text-white', badge: 'Pengguna' };

            const roleBadge = document.getElementById('viewRoleBadge');
            roleBadge.className = `px-3 py-1.5 rounded-full text-xs font-semibold ${roleConfig.bg} ${roleConfig.text}`;
            roleBadge.textContent = roleConfig.badge;

            // Set status
            document.getElementById('viewUserStatus').textContent = user.status.charAt(0).toUpperCase() + user.status.slice(1);

            const statusConfig = {
              'active': { bg: 'bg-green-100', text: 'text-green-800', icon: 'fas fa-check-circle' },
              'inactive': { bg: 'bg-red-100', text: 'text-red-800', icon: 'fas fa-times-circle' }
            }[user.status] || { bg: 'bg-gray-100', text: 'text-gray-800', icon: 'fas fa-question-circle' };

            const statusBadge = document.getElementById('viewStatusBadge');
            statusBadge.className = `px-3 py-1.5 rounded-full text-xs font-semibold ${statusConfig.text} ${statusConfig.bg} flex items-center gap-2`;
            statusBadge.innerHTML = `<i class="${statusConfig.icon}"></i>${user.status.charAt(0).toUpperCase() + user.status.slice(1)}`;

            // Set status affiliate
            const affiliateStatus = user.affiliate_status || 'inactive';
            document.getElementById('viewAffiliateStatus').textContent = affiliateStatus.charAt(0).toUpperCase() + affiliateStatus.slice(1);

            const affiliateConfig = {
              'inactive': { bg: 'bg-gray-100', text: 'text-gray-800', icon: 'fas fa-minus-circle' },
              'pending': { bg: 'bg-yellow-100', text: 'text-yellow-800', icon: 'fas fa-clock' },
              'approved': { bg: 'bg-green-100', text: 'text-green-800', icon: 'fas fa-check-circle' },
              'rejected': { bg: 'bg-red-100', text: 'text-red-800', icon: 'fas fa-times-circle' }
            }[affiliateStatus] || { bg: 'bg-gray-100', text: 'text-gray-800', icon: 'fas fa-minus-circle' };

            const affiliateBadge = document.getElementById('viewAffiliateBadge');
            affiliateBadge.className = `px-3 py-1.5 rounded-full text-xs font-semibold ${affiliateConfig.text} ${affiliateConfig.bg} flex items-center gap-2`;
            affiliateBadge.innerHTML = `<i class="${affiliateConfig.icon}"></i>${affiliateStatus.charAt(0).toUpperCase() + affiliateStatus.slice(1)}`;

            // Set tanggal
            const joinDate = user.created_at ? new Date(user.created_at).toLocaleDateString('id-ID', {
              weekday: 'long',
              day: 'numeric',
              month: 'long',
              year: 'numeric',
              hour: '2-digit',
              minute: '2-digit'
            }) : '-';
            document.getElementById('viewJoinDate').textContent = joinDate;

            // Set tombol edit ke pengguna saat ini
            document.getElementById('editFromViewBtn').setAttribute('onclick', `closeViewModal(); editUser(${user.id})`);

            // Tampilkan modal
            document.getElementById('viewModal').classList.remove('hidden');
            document.getElementById('viewModal').classList.add('flex');
            document.body.style.overflow = 'hidden';
          }
        }

        function closeViewModal() {
          document.getElementById('viewModal').classList.add('hidden');
          document.getElementById('viewModal').classList.remove('flex');
          document.body.style.overflow = '';
        }

        function editFromView() {
          closeViewModal();
          // Tunggu sedikit sebelum membuka modal edit agar animasi smooth
          setTimeout(() => {
            const userId = document.getElementById('viewUserID').textContent;
            editUser(parseInt(userId));
          }, 300);
        }

        function editUser(userId) {
          currentUserId = userId;
          const user = allUsers.find(u => u.id === userId);
          if (user) {
            document.getElementById('editUserId').value = user.id;
            document.getElementById('editUserID').textContent = user.id;
            document.getElementById('editName').value = user.name;
            document.getElementById('editEmail').value = user.email;
            document.getElementById('editRole').value = user.role_name || 'user';

            // Set avatar
            const avatar = document.getElementById('editUserAvatar');
            avatar.textContent = user.name.charAt(0).toUpperCase();
            avatar.className = 'w-20 h-20 user-avatar rounded-full flex items-center justify-center text-white text-2xl font-bold mb-4';

            // Set radio status
            document.querySelector(`input[name="editStatus"][value="${user.status}"]`).checked = true;

            // Set status affiliate jika ada
            if (user.affiliate_status) {
              document.getElementById('editAffiliateStatus').value = user.affiliate_status;
              document.getElementById('affiliateStatusSection').classList.remove('hidden');
            } else {
              document.getElementById('affiliateStatusSection').classList.add('hidden');
            }

            document.getElementById('editModal').classList.remove('hidden');
            document.getElementById('editModal').classList.add('flex');
            document.body.style.overflow = 'hidden';
          }
        }

        function closeEditModal() {
          currentUserId = null;
          document.getElementById('editModal').classList.add('hidden');
          document.getElementById('editModal').classList.remove('flex');
          document.body.style.overflow = '';
        }

        // Handle pengiriman form dengan konfirmasi
        document.getElementById('editForm').addEventListener('submit', async (e) => {
          e.preventDefault();

          const userId = currentUserId;
          if (!userId) return;

          const user = allUsers.find(u => u.id === userId);
          if (!user) return;

          // Tampilkan modal konfirmasi sebelum edit
          showConfirmationModal(
            'edit',
            'Konfirmasi Edit Pengguna',
            `Apakah Anda yakin ingin mengubah data pengguna "${user.name}"?`,
            'Ya, Edit',
            'Batal',
            async function () {
              const formData = {
                name: document.getElementById('editName').value,
                email: document.getElementById('editEmail').value,
                role: document.getElementById('editRole').value,
                status: document.querySelector('input[name="editStatus"]:checked').value,
                affiliate_status: document.getElementById('editAffiliateStatus').value
              };

              try {
                const response = await fetch(`/api/admin/users/${userId}`, {
                  method: 'PUT',
                  headers: { 'Content-Type': 'application/json' },
                  body: JSON.stringify(formData)
                });

                const data = await response.json();

                if (data.success) {
                  // Tampilkan notifikasi berhasil edit
                  showNotification('edit', 'Berhasil Diperbarui',
                    `Pengguna "${formData.name}" telah berhasil diperbarui.`);

                  closeEditModal();
                  loadUsers();
                } else {
                  showNotification('error', 'Gagal Memperbarui',
                    `Gagal mengupdate pengguna: ${data.message || 'Error tidak diketahui'}`);
                }
              } catch (error) {
                console.error('Error updating user:', error);
                showNotification('error', 'Gagal Memperbarui',
                  `Terjadi kesalahan: ${error.message}`);
              }
            },
            function () {
              // Aksi ketika dibatalkan
              console.log('Edit dibatalkan');
            }
          );
        });

        async function deleteUser(userId) {
          currentUserId = userId;
          const user = allUsers.find(u => u.id === userId);
          if (!user) return;

          // Tampilkan modal konfirmasi hapus
          showDeleteModal(
            user.name,
            async function () {
              try {
                const response = await fetch(`/api/admin/users/${userId}`, {
                  method: 'DELETE'
                });

                const data = await response.json();

                if (data.success) {
                  // Tampilkan notifikasi berhasil hapus
                  showNotification('delete', 'Berhasil Dihapus',
                    `Pengguna "${user.name}" telah berhasil dihapus.`);

                  loadUsers();
                } else {
                  showNotification('error', 'Gagal Menghapus',
                    `Gagal menghapus pengguna: ${data.message || 'Error tidak diketahui'}`);
                }
              } catch (error) {
                console.error('Error deleting user:', error);
                showNotification('error', 'Gagal Menghapus',
                  `Terjadi kesalahan: ${error.message}`);
              }
            },
            function () {
              // Aksi ketika dibatalkan
              console.log('Delete dibatalkan');
            }
          );
        }

        // Pencarian dan filter
        let searchTimeout;
        document.getElementById('searchInput').addEventListener('input', (e) => {
          clearTimeout(searchTimeout);
          searchTimeout = setTimeout(() => {
            filterUsers();
          }, 300);
        });

        document.getElementById('roleFilter').addEventListener('change', filterUsers);
        document.getElementById('statusFilter').addEventListener('change', filterUsers);

        function filterUsers() {
          const search = document.getElementById('searchInput').value.toLowerCase();
          const role = document.getElementById('roleFilter').value;
          const status = document.getElementById('statusFilter').value;

          const filtered = allUsers.filter(user => {
            const matchSearch = user.name.toLowerCase().includes(search) ||
              user.email.toLowerCase().includes(search);
            const matchRole = !role || user.role_name === role;
            const matchStatus = !status || user.status === status;
            return matchSearch && matchRole && matchStatus;
          });

          displayUsers(filtered);
        }

        function clearFilters() {
          document.getElementById('searchInput').value = '';
          document.getElementById('roleFilter').value = '';
          document.getElementById('statusFilter').value = '';
          filterUsers();
        }

        // Tutup modal dengan tombol escape
        document.addEventListener('keydown', (e) => {
          if (e.key === 'Escape') {
            if (!document.getElementById('viewModal').classList.contains('hidden')) {
              closeViewModal();
            } else if (!document.getElementById('editModal').classList.contains('hidden')) {
              closeEditModal();
            } else if (document.getElementById('confirmationModal').style.display === 'flex') {
              closeConfirmationModal();
            } else if (document.getElementById('deleteModal').style.display === 'flex') {
              closeDeleteModal();
            } else if (document.getElementById('notificationModal').style.display === 'flex') {
              closeNotification();
            }
          }
        });

        // Tutup modal dengan klik di luar
        document.getElementById('viewModal').addEventListener('click', (e) => {
          if (e.target === document.getElementById('viewModal')) {
            closeViewModal();
          }
        });

        document.getElementById('editModal').addEventListener('click', (e) => {
          if (e.target === document.getElementById('editModal')) {
            closeEditModal();
          }
        });

        // Event listener untuk modal notifikasi
        document.addEventListener('DOMContentLoaded', function () {
          // Event listener untuk tombol close pada notifikasi modal
          const closeBtn = document.getElementById('closeNotificationModalBtn');
          if (closeBtn) {
            closeBtn.addEventListener('click', function (e) {
              e.preventDefault();
              closeNotification();
            });
          }

          // Event listener untuk tombol close pada konfirmasi modal
          const cancelConfirmationBtn = document.getElementById('cancelConfirmationBtn');
          if (cancelConfirmationBtn) {
            cancelConfirmationBtn.addEventListener('click', function (e) {
              e.preventDefault();
              closeConfirmationModal();
            });
          }

          // Event listener untuk tombol close pada delete modal
          const cancelDeleteBtn = document.getElementById('cancelDeleteBtn');
          if (cancelDeleteBtn) {
            cancelDeleteBtn.addEventListener('click', function (e) {
              e.preventDefault();
              closeDeleteModal();
            });
          }

          // Tutup modal ketika klik di luar
          const notificationModal = document.getElementById('notificationModal');
          if (notificationModal) {
            notificationModal.addEventListener('click', function (e) {
              if (e.target === notificationModal) {
                closeNotification();
              }
            });
          }

          const confirmationModal = document.getElementById('confirmationModal');
          if (confirmationModal) {
            confirmationModal.addEventListener('click', function (e) {
              if (e.target === confirmationModal) {
                closeConfirmationModal();
              }
            });
          }

          const deleteModal = document.getElementById('deleteModal');
          if (deleteModal) {
            deleteModal.addEventListener('click', function (e) {
              if (e.target === deleteModal) {
                closeDeleteModal();
              }
            });
          }

          const detailModal = document.getElementById('viewModal');
          if (detailModal) {
            detailModal.addEventListener('click', (e) => {
              if (e.target === detailModal) {
                closeViewModal();
              }
            });
          }

          const approvalModal = document.getElementById('editModal');
          if (approvalModal) {
            approvalModal.addEventListener('click', (e) => {
              if (e.target === approvalModal) {
                closeEditModal();
              }
            });
          }
        });

        // Load users pada saat halaman dimuat
        document.addEventListener('DOMContentLoaded', loadUsers);

        // Auto-refresh setiap 60 detik
        setInterval(loadUsers, 60000);
      </script>
</body>

</html>